# Network

Project from Harvard course CS50W Web Programming with Python and JavaScript.

To run the project first make the migrations: `python manage.py makemigrations` and apply them `python manage.py migrate`. Then you can run it with `python manage.py runserver`.

Use `sass recipes/static/recipes/scss/styles.scss recipes/static/recipes/css/styles.css` to upload the css.
